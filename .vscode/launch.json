{
    "configurations": [
        {
            "name": "Launch built-in server and Debug",
            "detail": "Launches PHP's built-in development server and starts debugging.",
            "type": "php",
            "request": "launch",
            "noDebug": false,
            "runtimeArgs": [
                "-S",
                "localhost:8000",
                "-t",
                "client"
            ],
            "serverReadyAction": {
                "action": "openExternally"
            }
        }
    ]
}